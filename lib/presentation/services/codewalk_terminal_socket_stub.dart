import 'codewalk_terminal_socket.dart';

Future<CodewalkTerminalSocketConnection> openCodewalkTerminalSocketImpl({
  required Uri url,
  Map<String, String>? headers,
}) {
  throw UnsupportedError('Terminal websocket is not available here.');
}
