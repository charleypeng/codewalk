<?xml version="1.0" encoding="utf-8"?>
<paths>
  <!-- Expose the app's cache directory so the system installer can read the APK -->
  <cache-path name="cache" path="." />
  <external-cache-path name="external_cache" path="." />
</paths>
